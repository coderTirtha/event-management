import { Helmet } from "react-helmet-async";

const Blogs = () => {
    return (
        <div>
            <Helmet>
                <title>Rock N Roll | Blogs</title>
            </Helmet>
        </div>
    );
};

export default Blogs;